node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts.bak

# Bbox cache rebuilt on every `pnpm manifest` — keyed by mtime,
# committing it just churns. src/lib/manifest.json IS checked in:
# the CI Docker build can't see assets/ (gitignored) so the
# committed manifest is what the deployed bundle ships with.
.manifest-bbox-cache.json

# Symlinks to asset trees (point at gitignored content)
public/glb
public/raw
